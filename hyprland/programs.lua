-- Program variables
-- https://wiki.hypr.land/Configuring/Keywords/
-- Declared as locals here; keybinds.lua must be require()'d after this file.

terminal    = "kitty -1"
fileManager = "thunar"
menu        = "wofi --show drun --allow-images --parse-search --matching strict-contains"
browser     = "brave --password-store=basic --force-device-scale-factor=1.2"
